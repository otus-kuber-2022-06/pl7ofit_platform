---
apiVersion: v1
kind: Service
metadata:
  name: web-svc-lb
  annotations:
    metallb.universe.tf/address-pool: default
spec:
  selector:
    app: web
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  externalTrafficPolicy: Local
